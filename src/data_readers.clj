;; Keys are Namespace/Tag.
;; Values are the fully-namespace-qualified function for parsing the literals
;; (after they're read)

;; This is really more a sample than anything else.
{frereth/world frereth-client.parsers/world}
